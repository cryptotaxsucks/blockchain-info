import Form from "@/components/Form";

const page = () => {
  return (
    <main className="flex min-h-[90.4397vh] flex-col items-center justify-center">
      <Form />
    </main>
  );
};

export default page;
